Add inventory number to each postcard title

PostcardManager
  (All of this syncs to database)
- Update postcard data
- Remove postcard
- Get postcards from database
    - More specifically the ones that are requested. For example, fetching certain filters of postcards rather than fetching the entire collection.

Firestore
- userID
    - Postcards
        - General list of postcards
    - Batches
        - List of postcard document ids
        - Other batch info

Navigation tabs:
- Home
    - Large CTA, import postcards
    - Main filters, needs review, ready to list
        - Each send to "Collection" tab with the respective filters
    - Recent 5 batches
        - Each send to "Collection" tab with the respective filters
- Collection
    - Library/gallery view
    - Chip filter system
        - When sent here from external links, add the respective filter as a chip on the top.
        - No chips = all postcards/unfiltered
    - On click postcard (if its already listed, it would have to update eBay), display an editing panel on the right
        - This panel has fields to edit each value
        - On the bottom there will be a "save changes" button, and a "publish to ebay" button if all fields are filled out (ready to list), if postcard is listed already, the button is "view on ebay"
    - Fixed to the bottom of the gallery there will be a "Publish all ready to list" or something similar
    - Search bar (im not sure if i should add sorting, ai thinks i should)
    - Select button to select postcards to publish (instead of indivudally publishing or publishing all). Postcards that can't be published (needs review, listed) will be grayed out
- Batches
    - Displays all batches in a vertical stack
        - Each batch is a horizontal row
            - Each row will contain a date that it was imported to the app and the amount of postcards
                - Also, add thumbnail with overview of images, like Apple Music, as well as chip overview of postcard status
            - Each row, when clicked, will navigate the user to the "Collection" with a filter (chip) being applied. For example, it will be just like the normal "Collection" page but with a chip, i.e., "Batch: 10/21/25" being selected

Authentication
- contains userRef computed property (if authorized). see PostcardBatchService

delete batch id from batches array

fix indexes

